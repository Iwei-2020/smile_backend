<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.smile.backend.mapper.BaseDataMapper">
    <select id="getBaseData" resultType="com.smile.backend.vo.BaseDataVo">
        select *
        from (select COUNT(*) libraryCount from library) as library,
             (select COUNT(*) as imageCount from image) image,
             (select COUNT(*) as userCount from `user`) user;
    </select>

</mapper>